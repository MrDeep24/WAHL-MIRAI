@{
    Layout = (User.IsInRole(Roles.AdminName) || User.IsInRole(Roles.SuperAdminName))
        ? "_AdminLayout"
        : (User.Identity?.IsAuthenticated == true ? "_ElectorLayout" : "_Layout");

    ViewData["Title"] = "Ayuda";
}

@* ── Cabecera pública mínima: solo visible para visitantes anónimos ── *@
@if (User.Identity?.IsAuthenticated != true)
{
    <header class="flex items-center justify-between px-4 md:px-8 py-3 border-b border-outline/20 bg-surface-container-lowest">
        <a href="/" class="flex items-center gap-2 hover:opacity-80 transition-opacity">
            <img src="/images/logo.png" alt="Wahl Mirai Logo" class="w-7 h-7 rounded-md object-cover" />
            <span class="font-bold text-primary text-base">Wahl Mirai</span>
        </a>
        <nav class="flex items-center gap-4 text-sm font-medium">
            <a href="/" class="text-on-surface-variant hover:text-primary transition-colors">Inicio</a>
            <a href="/Auth/Login" class="px-4 py-1.5 rounded-lg bg-primary text-on-primary hover:opacity-90 transition-opacity">Iniciar sesión</a>
        </nav>
    </header>
}

<div class="p-4 md:p-8 max-w-6xl mx-auto w-full">

    <div class="mb-6">
        <h1 class="text-2xl font-bold mb-2">Ayuda y Asistencia</h1>
        <p class="text-on-surface-variant">Aquí encontrarás respuestas a las preguntas más frecuentes sobre el sistema y podrás interactuar con nuestro asistente virtual.</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start">

        @* ── Columna Izquierda: Acordeón FAQ + CTA + Historial ── *@
        <div class="lg:col-span-7 space-y-6">

            <section class="space-y-3">

                @* 1. ¿Cómo creo mi cuenta? *@
                <details class="p-4 border border-outline/30 rounded-lg bg-surface-container-lowest">
                    <summary class="font-semibold cursor-pointer select-none">¿Cómo creo mi cuenta?</summary>
                    <div class="mt-2 text-sm text-on-surface-variant">
                        <button type="button"
                                onclick="document.getElementById('modal-registro').showModal()"
                                class="block w-full max-w-sm cursor-zoom-in hover:opacity-90 transition-opacity rounded-md overflow-hidden focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                aria-label="Ampliar diagrama: Cómo crear tu cuenta">
                            <img class="w-full" src="/img/ayuda/ayuda-registro.svg" alt="Diagrama de 3 pasos para crear tu cuenta" />
                        </button>
                        <small class="block text-xs text-on-surface-variant mt-2">Haz clic sobre la imagen para ampliarla</small>
                        <p class="mt-3">Ingresa tu número de documento en <strong>Crear mi cuenta</strong>. Si el colegio ya te habilitó en la lista de estudiantes autorizados, verás tu nombre y grado precargados; solo debes definir tu correo de contacto y una contraseña (mínimo 8 caracteres, una mayúscula y un símbolo especial). Si el sistema no reconoce tu documento, contacta al Administrador del colegio.</p>
                    </div>
                </details>

                @* 2. ¿Cómo inicio sesión? *@
                <details class="p-4 border border-outline/30 rounded-lg bg-surface-container-lowest">
                    <summary class="font-semibold cursor-pointer select-none">¿Cómo inicio sesión?</summary>
                    <div class="mt-2 text-sm text-on-surface-variant">
                        <button type="button"
                                onclick="document.getElementById('modal-login').showModal()"
                                class="block w-full max-w-sm cursor-zoom-in hover:opacity-90 transition-opacity rounded-md overflow-hidden focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                aria-label="Ampliar diagrama: Cómo iniciar sesión">
                            <img class="w-full" src="/img/ayuda/ayuda-login.svg" alt="Diagrama de 3 pasos para iniciar sesión" />
                        </button>
                        <small class="block text-xs text-on-surface-variant mt-2">Haz clic sobre la imagen para ampliarla</small>
                        <p class="mt-3">Ingresa tu número de documento y la contraseña que tú mismo definiste al crear tu cuenta. El sistema <strong>nunca</strong> te envía la contraseña por correo al registrarte: solo lo hace si usas la opción <strong>Recuperar acceso</strong>. Si los datos no coinciden, revisa que no haya espacios de más en el documento.</p>
                    </div>
                </details>

                @* 3. ¿Olvidé mi contraseña? *@
                <details class="p-4 border border-outline/30 rounded-lg bg-surface-container-lowest">
                    <summary class="font-semibold cursor-pointer select-none">¿Olvidé mi contraseña, qué hago?</summary>
                    <div class="mt-2 text-sm text-on-surface-variant">
                        <button type="button"
                                onclick="document.getElementById('modal-recuperar').showModal()"
                                class="block w-full max-w-sm cursor-zoom-in hover:opacity-90 transition-opacity rounded-md overflow-hidden focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                aria-label="Ampliar diagrama: Cómo recuperar tu contraseña">
                            <img class="w-full" src="/img/ayuda/ayuda-recuperar.svg" alt="Diagrama de 3 pasos para recuperar tu contraseña" />
                        </button>
                        <small class="block text-xs text-on-surface-variant mt-2">Haz clic sobre la imagen para ampliarla</small>
                        <p class="mt-3">Ve a <strong>Recuperar acceso</strong> e ingresa tu documento. Te enviaremos una nueva contraseña al correo de contacto que registraste al crear tu cuenta.</p>
                    </div>
                </details>

                @* 4. ¿Cómo me postulo como candidato? — Fix 2: ahora con diagrama e ilustración *@
                <details class="p-4 border border-outline/30 rounded-lg bg-surface-container-lowest">
                    <summary class="font-semibold cursor-pointer select-none">¿Cómo me postulo como candidato?</summary>
                    <div class="mt-2 text-sm text-on-surface-variant">
                        <button type="button"
                                onclick="document.getElementById('modal-postulacion').showModal()"
                                class="block w-full max-w-sm cursor-zoom-in hover:opacity-90 transition-opacity rounded-md overflow-hidden focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                aria-label="Ampliar diagrama: Cómo postularte como candidato">
                            <img class="w-full" src="/img/ayuda/ayuda-postulacion.svg" alt="Diagrama de 3 pasos para postularte como candidato" />
                        </button>
                        <small class="block text-xs text-on-surface-variant mt-2">Haz clic sobre la imagen para ampliarla</small>
                        <p class="mt-3">Durante la etapa de <strong>Inscripción</strong> de una elección, entra a <strong>Postularme como candidato</strong>, carga tu foto, tus propuestas y tu plan de gobierno, además de los documentos que exija el cargo al que aspiras. Tu postulación queda <em>pendiente</em> hasta que el Administrador la revise y apruebe; solo entonces aparecerás en el tarjetón.</p>
                    </div>
                </details>

                @* 5. ¿Cómo voto? *@
                <details class="p-4 border border-outline/30 rounded-lg bg-surface-container-lowest">
                    <summary class="font-semibold cursor-pointer select-none">¿Cómo voto?</summary>
                    <div class="mt-2 text-sm text-on-surface-variant">
                        <button type="button"
                                onclick="document.getElementById('modal-votar').showModal()"
                                class="block w-full max-w-sm cursor-zoom-in hover:opacity-90 transition-opacity rounded-md overflow-hidden focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                aria-label="Ampliar diagrama: Cómo votar">
                            <img class="w-full" src="/img/ayuda/ayuda-votar.svg" alt="Diagrama de 3 pasos para votar" />
                        </button>
                        <small class="block text-xs text-on-surface-variant mt-2">Haz clic sobre la imagen para ampliarla</small>
                        <p class="mt-3">Cuando la elección esté en su etapa de <strong>Votación</strong>, elige un candidato en el tarjetón, revisa sus propuestas en la ventana emergente y confirma tu voto. Una vez confirmado, no se puede deshacer.</p>
                    </div>
                </details>

                @* 6. ¿Cómo edito mi perfil? *@
                <details class="p-4 border border-outline/30 rounded-lg bg-surface-container-lowest">
                    <summary class="font-semibold cursor-pointer select-none">¿Cómo edito mi perfil?</summary>
                    <div class="mt-2 text-sm text-on-surface-variant">
                        <button type="button"
                                onclick="document.getElementById('modal-perfil').showModal()"
                                class="block w-full max-w-sm cursor-zoom-in hover:opacity-90 transition-opacity rounded-md overflow-hidden focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                aria-label="Ampliar diagrama: Cómo editar tu perfil">
                            <img class="w-full" src="/img/ayuda/ayuda-perfil.svg" alt="Diagrama de 3 pasos para editar tu perfil" />
                        </button>
                        <small class="block text-xs text-on-surface-variant mt-2">Haz clic sobre la imagen para ampliarla</small>
                        <p class="mt-3">Puedes cambiar tu correo de contacto o tu contraseña desde <strong>Mi perfil</strong>. Tu documento, nombre y grado los administra el colegio y no se pueden editar.</p>
                    </div>
                </details>

                @* 7. ¿Cuándo puedo ver los resultados? *@
                <details class="p-4 border border-outline/30 rounded-lg bg-surface-container-lowest">
                    <summary class="font-semibold cursor-pointer select-none">¿Cuándo puedo ver los resultados?</summary>
                    <div class="mt-2 text-sm text-on-surface-variant">
                        <button type="button"
                                onclick="document.getElementById('modal-resultados').showModal()"
                                class="block w-full max-w-sm cursor-zoom-in hover:opacity-90 transition-opacity rounded-md overflow-hidden focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                aria-label="Ampliar diagrama: Cuándo ver los resultados">
                            <img class="w-full" src="/img/ayuda/ayuda-resultados.svg" alt="Diagrama de 3 pasos para ver los resultados" />
                        </button>
                        <small class="block text-xs text-on-surface-variant mt-2">Haz clic sobre la imagen para ampliarla</small>
                        <p class="mt-3">Mientras la elección está activa, solo ves resultados si ya votaste. Al finalizar, los resultados se abren para todos los electores del grado habilitado.</p>
                    </div>
                </details>

            </section>

            @* ── CTA: Crear PQR condicional según rol (solo ELECTOR) o invitación a iniciar sesión ── *@
            @if (User.IsInRole(Roles.ElectorName))
            {
                <div class="pt-2">
                    <a href="/Pqr/Create" class="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-primary text-on-primary hover:opacity-90 transition-opacity font-medium">
                        <span class="material-symbols-outlined text-[18px]">chat_bubble</span>
                        Crear PQR
                    </a>
                </div>
            }
            else if (User.Identity?.IsAuthenticated != true)
            {
                <div class="pt-2">
                    <p class="text-sm text-on-surface-variant">
                        ¿Necesitas radicar una solicitud formal?
                        <a href="/Auth/Login" class="text-primary font-medium hover:underline">Inicia sesión para crear una PQR.</a>
                    </p>
                </div>
            }

            @* ── Historial de PQR del elector — sin cambios en condición ni en llamada a /Pqr/Mine ── *@
            @if (User.IsInRole(Roles.ElectorName))
            {
                <div class="pt-6 border-t border-outline/20">
                    <h2 class="text-xl font-bold mb-3">Mis Solicitudes</h2>
                    <div data-pqr-mine-list class="mt-4 flex flex-col gap-2"></div>
                </div>
            }

        </div>

        @* ── Columna Derecha: Panel del Chatbot de Ayuda (RF-M08-03) ── *@
        <div class="lg:col-span-5">
            <div data-chatbot-panel
                 data-user-authenticated="@(User.Identity?.IsAuthenticated == true ? "true" : "false")"
                 data-user-is-elector="@(User.IsInRole(Roles.ElectorName) ? "true" : "false")"
                 data-user-is-admin="@((User.IsInRole(Roles.AdminName) || User.IsInRole(Roles.SuperAdminName)) ? "true" : "false")"
                 class="border border-outline/30 rounded-lg bg-surface-container-lowest p-4 md:p-5 shadow-sm flex flex-col h-[600px] sticky top-6">

                @* Cabecera del Chatbot *@
                <div class="flex items-center justify-between pb-3 border-b border-outline/20">
                    <div class="flex items-center gap-2">
                        <div class="w-8 h-8 rounded-full bg-primary-container text-on-primary-container flex items-center justify-center">
                            <span class="material-symbols-outlined text-[18px]">smart_toy</span>
                        </div>
                        <div>
                            <h2 class="font-semibold text-primary text-sm leading-tight">Asistente de Ayuda</h2>
                            <span class="text-[11px] text-secondary flex items-center gap-1 font-medium">
                                <span class="w-1.5 h-1.5 rounded-full bg-secondary inline-block"></span> En línea (Automático)
                            </span>
                        </div>
                    </div>
                    <button type="button" data-chatbot-reset class="text-xs text-on-surface-variant hover:text-primary transition-colors flex items-center gap-1" title="Reiniciar conversación">
                        <span class="material-symbols-outlined text-[16px]">restart_alt</span>
                        <span class="hidden sm:inline">Reiniciar</span>
                    </button>
                </div>

                @* Área de Mensajes *@
                <div data-chatbot-messages class="flex-1 overflow-y-auto space-y-3 py-3 pr-1"></div>

                @* Menú rápido de temas (chips) *@
                <div class="pt-2 border-t border-outline/20">
                    <p class="text-[11px] font-medium text-on-surface-variant mb-1.5">Temas rápidos:</p>
                    <div data-chatbot-menu class="flex flex-wrap gap-1.5 max-h-20 overflow-y-auto pb-1">
                        <button type="button" data-chatbot-topic="registro" class="text-xs px-2.5 py-1 rounded-full bg-surface-container hover:bg-surface-container-high text-on-surface transition-colors border border-outline/20">Crear cuenta</button>
                        <button type="button" data-chatbot-topic="login" class="text-xs px-2.5 py-1 rounded-full bg-surface-container hover:bg-surface-container-high text-on-surface transition-colors border border-outline/20">Iniciar sesión</button>
                        <button type="button" data-chatbot-topic="recuperar" class="text-xs px-2.5 py-1 rounded-full bg-surface-container hover:bg-surface-container-high text-on-surface transition-colors border border-outline/20">Recuperar clave</button>
                        <button type="button" data-chatbot-topic="votar" class="text-xs px-2.5 py-1 rounded-full bg-surface-container hover:bg-surface-container-high text-on-surface transition-colors border border-outline/20">Cómo votar</button>
                        <button type="button" data-chatbot-topic="postulacion" class="text-xs px-2.5 py-1 rounded-full bg-surface-container hover:bg-surface-container-high text-on-surface transition-colors border border-outline/20">Postulación</button>
                        <button type="button" data-chatbot-topic="perfil" class="text-xs px-2.5 py-1 rounded-full bg-surface-container hover:bg-surface-container-high text-on-surface transition-colors border border-outline/20">Mi perfil</button>
                        <button type="button" data-chatbot-topic="resultados" class="text-xs px-2.5 py-1 rounded-full bg-surface-container hover:bg-surface-container-high text-on-surface transition-colors border border-outline/20">Resultados</button>
                    </div>
                </div>

                @* Formulario de entrada de texto libre *@
                <form data-chatbot-form class="flex items-center gap-2 pt-2 border-t border-outline/20">
                    <input data-chatbot-input type="text" placeholder="Escribe una palabra clave..." class="flex-1 bg-surface-container-lowest border border-outline/30 rounded-lg px-3 py-2 text-xs md:text-sm text-on-surface focus:outline-none focus:border-primary placeholder:text-on-surface-variant/60" />
                    <button type="submit" data-chatbot-send class="px-3 py-2 rounded-lg bg-primary text-on-primary hover:opacity-90 transition-opacity text-xs md:text-sm flex items-center justify-center font-medium" title="Enviar mensaje">
                        <span class="material-symbols-outlined text-[18px]">send</span>
                    </button>
                </form>

            </div>
        </div>

    </div>

</div>

@* ══════════════════════════════════════════════════════════════════════════
   Modales nativos <dialog> — Ampliación de diagramas FAQ
   Fix 3: m-auto — Tailwind Preflight elimina el margin:auto UA del <dialog>
   Fix 4: overflow-x-auto + min-w-[650px] — usabilidad en móvil
   RF-M08 Accesibilidad & UX — 2026-09-14
   ══════════════════════════════════════════════════════════════════════════ *@

@* Modal: Crear cuenta *@
<dialog id="modal-registro"
        class="max-w-4xl w-full p-0 m-auto rounded-xl shadow-2xl border-0 backdrop:bg-black/80 open:animate-fade-in">
    <div class="bg-surface-container-lowest rounded-xl overflow-hidden">
        <div class="flex items-center justify-between px-5 py-3 border-b border-outline/20">
            <h3 class="font-semibold text-on-surface text-sm">¿Cómo creo mi cuenta?</h3>
            <form method="dialog">
                <button type="submit"
                        class="flex items-center gap-1 text-xs text-on-surface-variant hover:text-primary transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary rounded"
                        aria-label="Cerrar diagrama de registro">
                    <span class="material-symbols-outlined text-[18px]">close</span>
                    <span class="hidden sm:inline">Cerrar</span>
                </button>
            </form>
        </div>
        <div class="p-4">
            <div class="w-full overflow-x-auto">
                <img class="min-w-[650px] md:min-w-full h-auto"
                     src="/img/ayuda/ayuda-registro.svg"
                     alt="Diagrama ampliado de 3 pasos para crear tu cuenta" />
            </div>
            <p class="sm:hidden text-xs text-on-surface-variant text-center mt-2 italic">Desliza hacia los lados para ver los 3 pasos</p>
        </div>
    </div>
</dialog>

@* Modal: Iniciar sesión *@
<dialog id="modal-login"
        class="max-w-4xl w-full p-0 m-auto rounded-xl shadow-2xl border-0 backdrop:bg-black/80 open:animate-fade-in">
    <div class="bg-surface-container-lowest rounded-xl overflow-hidden">
        <div class="flex items-center justify-between px-5 py-3 border-b border-outline/20">
            <h3 class="font-semibold text-on-surface text-sm">¿Cómo inicio sesión?</h3>
            <form method="dialog">
                <button type="submit"
                        class="flex items-center gap-1 text-xs text-on-surface-variant hover:text-primary transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary rounded"
                        aria-label="Cerrar diagrama de inicio de sesión">
                    <span class="material-symbols-outlined text-[18px]">close</span>
                    <span class="hidden sm:inline">Cerrar</span>
                </button>
            </form>
        </div>
        <div class="p-4">
            <div class="w-full overflow-x-auto">
                <img class="min-w-[650px] md:min-w-full h-auto"
                     src="/img/ayuda/ayuda-login.svg"
                     alt="Diagrama ampliado de 3 pasos para iniciar sesión" />
            </div>
            <p class="sm:hidden text-xs text-on-surface-variant text-center mt-2 italic">Desliza hacia los lados para ver los 3 pasos</p>
        </div>
    </div>
</dialog>

@* Modal: Recuperar contraseña *@
<dialog id="modal-recuperar"
        class="max-w-4xl w-full p-0 m-auto rounded-xl shadow-2xl border-0 backdrop:bg-black/80 open:animate-fade-in">
    <div class="bg-surface-container-lowest rounded-xl overflow-hidden">
        <div class="flex items-center justify-between px-5 py-3 border-b border-outline/20">
            <h3 class="font-semibold text-on-surface text-sm">¿Olvidé mi contraseña, qué hago?</h3>
            <form method="dialog">
                <button type="submit"
                        class="flex items-center gap-1 text-xs text-on-surface-variant hover:text-primary transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary rounded"
                        aria-label="Cerrar diagrama de recuperación de contraseña">
                    <span class="material-symbols-outlined text-[18px]">close</span>
                    <span class="hidden sm:inline">Cerrar</span>
                </button>
            </form>
        </div>
        <div class="p-4">
            <div class="w-full overflow-x-auto">
                <img class="min-w-[650px] md:min-w-full h-auto"
                     src="/img/ayuda/ayuda-recuperar.svg"
                     alt="Diagrama ampliado de 3 pasos para recuperar tu contraseña" />
            </div>
            <p class="sm:hidden text-xs text-on-surface-variant text-center mt-2 italic">Desliza hacia los lados para ver los 3 pasos</p>
        </div>
    </div>
</dialog>

@* Modal: Postulación como candidato — Fix 2: nuevo modal *@
<dialog id="modal-postulacion"
        class="max-w-4xl w-full p-0 m-auto rounded-xl shadow-2xl border-0 backdrop:bg-black/80 open:animate-fade-in">
    <div class="bg-surface-container-lowest rounded-xl overflow-hidden">
        <div class="flex items-center justify-between px-5 py-3 border-b border-outline/20">
            <h3 class="font-semibold text-on-surface text-sm">¿Cómo me postulo como candidato?</h3>
            <form method="dialog">
                <button type="submit"
                        class="flex items-center gap-1 text-xs text-on-surface-variant hover:text-primary transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary rounded"
                        aria-label="Cerrar diagrama de postulación">
                    <span class="material-symbols-outlined text-[18px]">close</span>
                    <span class="hidden sm:inline">Cerrar</span>
                </button>
            </form>
        </div>
        <div class="p-4">
            <div class="w-full overflow-x-auto">
                <img class="min-w-[650px] md:min-w-full h-auto"
                     src="/img/ayuda/ayuda-postulacion.svg"
                     alt="Diagrama ampliado de 3 pasos para postularte como candidato" />
            </div>
            <p class="sm:hidden text-xs text-on-surface-variant text-center mt-2 italic">Desliza hacia los lados para ver los 3 pasos</p>
        </div>
    </div>
</dialog>

@* Modal: Votar *@
<dialog id="modal-votar"
        class="max-w-4xl w-full p-0 m-auto rounded-xl shadow-2xl border-0 backdrop:bg-black/80 open:animate-fade-in">
    <div class="bg-surface-container-lowest rounded-xl overflow-hidden">
        <div class="flex items-center justify-between px-5 py-3 border-b border-outline/20">
            <h3 class="font-semibold text-on-surface text-sm">¿Cómo voto?</h3>
            <form method="dialog">
                <button type="submit"
                        class="flex items-center gap-1 text-xs text-on-surface-variant hover:text-primary transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary rounded"
                        aria-label="Cerrar diagrama de votación">
                    <span class="material-symbols-outlined text-[18px]">close</span>
                    <span class="hidden sm:inline">Cerrar</span>
                </button>
            </form>
        </div>
        <div class="p-4">
            <div class="w-full overflow-x-auto">
                <img class="min-w-[650px] md:min-w-full h-auto"
                     src="/img/ayuda/ayuda-votar.svg"
                     alt="Diagrama ampliado de 3 pasos para votar" />
            </div>
            <p class="sm:hidden text-xs text-on-surface-variant text-center mt-2 italic">Desliza hacia los lados para ver los 3 pasos</p>
        </div>
    </div>
</dialog>

@* Modal: Editar perfil *@
<dialog id="modal-perfil"
        class="max-w-4xl w-full p-0 m-auto rounded-xl shadow-2xl border-0 backdrop:bg-black/80 open:animate-fade-in">
    <div class="bg-surface-container-lowest rounded-xl overflow-hidden">
        <div class="flex items-center justify-between px-5 py-3 border-b border-outline/20">
            <h3 class="font-semibold text-on-surface text-sm">¿Cómo edito mi perfil?</h3>
            <form method="dialog">
                <button type="submit"
                        class="flex items-center gap-1 text-xs text-on-surface-variant hover:text-primary transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary rounded"
                        aria-label="Cerrar diagrama de edición de perfil">
                    <span class="material-symbols-outlined text-[18px]">close</span>
                    <span class="hidden sm:inline">Cerrar</span>
                </button>
            </form>
        </div>
        <div class="p-4">
            <div class="w-full overflow-x-auto">
                <img class="min-w-[650px] md:min-w-full h-auto"
                     src="/img/ayuda/ayuda-perfil.svg"
                     alt="Diagrama ampliado de 3 pasos para editar tu perfil" />
            </div>
            <p class="sm:hidden text-xs text-on-surface-variant text-center mt-2 italic">Desliza hacia los lados para ver los 3 pasos</p>
        </div>
    </div>
</dialog>

@* Modal: Resultados *@
<dialog id="modal-resultados"
        class="max-w-4xl w-full p-0 m-auto rounded-xl shadow-2xl border-0 backdrop:bg-black/80 open:animate-fade-in">
    <div class="bg-surface-container-lowest rounded-xl overflow-hidden">
        <div class="flex items-center justify-between px-5 py-3 border-b border-outline/20">
            <h3 class="font-semibold text-on-surface text-sm">¿Cuándo puedo ver los resultados?</h3>
            <form method="dialog">
                <button type="submit"
                        class="flex items-center gap-1 text-xs text-on-surface-variant hover:text-primary transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary rounded"
                        aria-label="Cerrar diagrama de resultados">
                    <span class="material-symbols-outlined text-[18px]">close</span>
                    <span class="hidden sm:inline">Cerrar</span>
                </button>
            </form>
        </div>
        <div class="p-4">
            <div class="w-full overflow-x-auto">
                <img class="min-w-[650px] md:min-w-full h-auto"
                     src="/img/ayuda/ayuda-resultados.svg"
                     alt="Diagrama ampliado de 3 pasos para ver los resultados" />
            </div>
            <p class="sm:hidden text-xs text-on-surface-variant text-center mt-2 italic">Desliza hacia los lados para ver los 3 pasos</p>
        </div>
    </div>
</dialog>

@section Scripts {
    <script src="~/js/ayuda-chatbot.js" asp-append-version="true"></script>

    @if (User.IsInRole(Roles.ElectorName))
    {
        <script>
            const mineList = document.querySelector('[data-pqr-mine-list]');

            function escapeHtml(value) {
                return String(value ?? '')
                    .replace(/&/g, '&amp;')
                    .replace(/</g, '&lt;')
                    .replace(/>/g, '&gt;')
                    .replace(/"/g, '&quot;')
                    .replace(/'/g, '&#039;');
            }

            function formatDate(value) {
                if (!value) return 'Sin fecha';
                const date = new Date(value);
                if (Number.isNaN(date.getTime())) return 'Sin fecha';
                return new Intl.DateTimeFormat('es-ES', {
                    day: '2-digit',
                    month: '2-digit',
                    year: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                }).format(date);
            }

            function statusLabel(status) {
                return status === 'RESUELTO' ? 'Resuelta' : 'Abierta';
            }

            function statusBadgeClass(status) {
                return status === 'RESUELTO'
                    ? 'bg-status-graduated/20 text-status-graduated'
                    : 'bg-status-pending/10 text-status-pending';
            }

            async function loadMineHistory() {
                if (!mineList) return;

                try {
                    const res = await fetch('/Pqr/Mine', {
                        method: 'GET',
                        headers: { 'Accept': 'application/json' }
                    });
                    const data = await res.json();

                    if (!data.ok) {
                        mineList.innerHTML = '<p class="text-sm text-on-surface-variant">No se pudo cargar tu historial de PQR.</p>';
                        return;
                    }

                    const tickets = Array.isArray(data.tickets) ? data.tickets : [];

                    if (!tickets.length) {
                        mineList.innerHTML = '<p class="text-sm text-on-surface-variant">Aún no has creado ninguna PQR.</p>';
                        return;
                    }

                    mineList.innerHTML = tickets.map((ticket) => {
                        const status = statusLabel(ticket.status);
                        const responseHtml = ticket.status === 'RESUELTO'
                            ? `<div class="mt-3 rounded-md bg-surface-container-low p-3 text-sm">
                                   <span class="font-medium text-on-surface">Respuesta del administrador:</span>
                                   <p class="mt-1 whitespace-pre-wrap text-on-surface-variant">${escapeHtml(ticket.adminResponse || '')}</p>
                               </div>`
                            : '';

                        return `
                            <article class="rounded-lg border border-outline/30 bg-surface p-4 shadow-sm">
                                <div class="flex items-start justify-between gap-3">
                                    <div class="min-w-0">
                                        <p class="font-medium text-on-surface break-words">${escapeHtml(ticket.subject || '')}</p>
                                        <p class="mt-1 text-xs text-on-surface-variant">${formatDate(ticket.createdAt)}</p>
                                    </div>
                                    <span class="inline-flex items-center rounded-full px-2 py-1 text-xs font-medium ${statusBadgeClass(ticket.status)}">${status}</span>
                                </div>
                                <p class="mt-3 text-sm text-on-surface-variant">${escapeHtml(ticket.message || '')}</p>
                                ${responseHtml}
                            </article>
                        `;
                    }).join('');
                } catch (err) {
                    console.error(err);
                    mineList.innerHTML = '<p class="text-sm text-on-surface-variant">No se pudo cargar tu historial de PQR.</p>';
                }
            }

            loadMineHistory();
        </script>
    }
}
